[package]
name = "nonos-chat-protocol-tests"
version = "0.1.0"
edition = "2021"
publish = false
[dependencies]
serde_json = { version = "1", default-features = false, features = ["alloc"] }
[lib]
path = "protocol.rs"
